import { api, track } from 'lwc';
import LightningModal from 'lightning/modal';

export default class Sandi_subscriptionServiceDetail extends LightningModal {
    handleClose(){
        this.close();
    }
}